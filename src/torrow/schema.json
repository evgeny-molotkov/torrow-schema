{
  "$schema": "http://json-schema.org/schema",
  "id": "TTForm",
  "title": "Torrow form",
  "type": "object",
  "description": "Creates a new torrow form component.",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the form component.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path at which to create the schema files, relative to the current workspace.",
      "visible": false
    }
  },
  "required": ["name"]
}
